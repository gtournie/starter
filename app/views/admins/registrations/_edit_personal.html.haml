.container-fluid
  %h1
    %span.fa.fa-user
    = t('.title')

  = simple_form_for resource, as: resource_name, url: send("update_#{resource_name}_registration_path"),
    html: { method: :put, class: "form-admin" } do |f|

    .row
      .col-xs-12
        = f.error_notification
        = f.input :last_name, autofocus: true
        = f.input :first_name
        = f.input :email, required: true
        - if devise_mapping.confirmable? && resource.pending_reconfirmation?
          %p= t('devise.waiting_for_email_confirmation', email: resource.unconfirmed_email)

    .row
      .col-md-4.col-md-offset-4.col-sm-6.col-sm-offset-3
        = f.button :submit